package com.string;

public class CharacterCountingProblems {

//    public static String findSubString(String input, String pattern) {
//
//        if (pattern.length() > input.length()) {
//            throw new IllegalArgumentException("window can't be found");
//        }
//
//        char[] patternArr = pattern.toCharArray();
//        int[] patternHash = new int[pattern.length()];
//
//
//        int count = 0;
//        for (int i = 0; i < input.length(); i++) {
//
//            if (input.charAt(i) == patternArr[count] ) {
//                patternHash[count] = i;
//                ++count;
//            }
//
//            if (count == patternArr.length) {
//                startIndex = patternHash[0];
//                minLength = patternHash[patternArr.length - 1] - patternHash[0];
//            }
//
//        }
//
//        if (startIndex == -1) {
//            throw new IllegalArgumentException("window not be found");
//        }
//
//        return input.substring(startIndex, startIndex + minLength);
//    }

    public static void main(String[] args) {

//        System.out.println(findSubString("this is a test string", "tist"));

    }
}
